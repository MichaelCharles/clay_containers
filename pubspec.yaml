name: clay_containers
description: Easily create custom neumorphic container widgets for your own unique design.
version: 0.3.4
homepage: https://github.com/mcaubrey/clay_containers

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^3.0.1
  flutter_test:
    sdk: flutter

flutter:
